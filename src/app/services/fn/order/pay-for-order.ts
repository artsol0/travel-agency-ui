/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DataResponseOrderDto } from '../../models/data-response-order-dto';

export interface PayForOrder$Params {
  voucherId: string;
  userId: string;
}

export function payForOrder(http: HttpClient, rootUrl: string, params: PayForOrder$Params, context?: HttpContext): Observable<StrictHttpResponse<DataResponseOrderDto>> {
  const rb = new RequestBuilder(rootUrl, payForOrder.PATH, 'put');
  if (params) {
    rb.path('voucherId', params.voucherId, {});
    rb.path('userId', params.userId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DataResponseOrderDto>;
    })
  );
}

payForOrder.PATH = '/api/v1/orders/pay/{voucherId}/{userId}';
